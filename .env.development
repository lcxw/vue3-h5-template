# baseUrl
# /dev-api 为本地 mock 地址，不使用 mock 的话此处换为你的开发环境接口地址
VITE_BASE_API = '/dev-api'

# 开发环境启用 cdn eruda 调试工具。若不启用，将 true 修改为 false 或其他任意值即可
VITE_ENABLE_ERUDA = "true"

# 线上环境平台打包路径
VITE_PUBLIC_PATH = /

# OnlyOffice 服务地址
VITE_ONLYOFFICE_URL = "http://10.10.10.211:9003"

# 项目名称
VITE_PROJECT_NAME = "项目管理系统"
